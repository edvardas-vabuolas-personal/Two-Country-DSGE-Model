\ProvidesPackage{Packages}
\usepackage{graphicx}
\usepackage[a4paper,
            left=1.25in,
            right=1in,
            top=1.5in,
            bottom=1.5in]{geometry}
\usepackage{xcolor}
\usepackage{import}
\usepackage{setspace}
\usepackage{amsmath, calc, amsfonts}
\usepackage{float}
\usepackage{tikz}
\usepackage{xfrac}
\usepackage[makeroom]{cancel}
\usepackage[style=authoryear,sorting=nyt,natbib,backend=biber, url=false]{biblatex}
\DefineBibliographyStrings{english}{%
  page             = {},
  pages            = {},
} 
\renewcommand{\postnotedelim}{\addcolon}
% \addbibresource{DissertationDSGE.bib}
\usepackage{tabularx}
\usepackage{pdflscape}
\usepackage{makecell}

\DeclareMathOperator{\E}{\mathbb{E}}

\usepackage{verbatim}

\usepackage{dashbox}
\newcommand\dboxed[1]{\dbox{\ensuremath{#1}}}

\newcommand*\xbar[1]{%
  \hbox{%
    \vbox{%
      \hrule height 0.5pt % The actual bar
      \kern0.5ex%         % Distance between bar and symbol
      \hbox{%
        \kern-0.1em%      % Shortening on the left side
        \ensuremath{#1}%
        \kern-0.1em%      % Shortening on the right side
      }%
    }%
  }%
} 
\endinput