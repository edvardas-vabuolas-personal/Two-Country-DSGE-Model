\ProvidesPackage{Packages}
\usepackage{graphicx}
\usepackage[a4paper,
            left=1.25in,
            right=1in,
            top=1.5in,
            bottom=1.5in]{geometry}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{import}
\usepackage{setspace}
\usepackage{amsmath, calc, amsfonts}
\usepackage{float}
\usepackage{tikz}
\usepackage{xfrac}
\usepackage[makeroom]{cancel}
\usepackage[style=authoryear-icomp,sorting=nyt,natbib,backend=biber, url=false, ibidpage=true, uniquename=false]{biblatex}
\renewcommand*{\mkibid}{\emph}

\newtoggle{cbx:isibid}

\AtEveryCitekey{\global\togglefalse{cbx:isibid}}

\renewbibmacro*{cite:ibid}{%
  \global\toggletrue{cbx:isibid}%
  \printtext[bibhyperref]{\bibstring[\mkibid]{ibidem}}%
  \ifloccit
    {\global\booltrue{cbx:loccit}}
    {}}

\DeclareDelimFormat*{postnotedelim}{%
  \iftoggle{cbx:isibid}
    {\addcomma\space p.\space}
    {\addcolon}%
  }

\DeclareFieldFormat{postnote}{\mknormrange{#1}}
\DeclareFieldFormat{volcitepages}{\mknormrange{#1}}

\usepackage{tabularx}
\usepackage{pdflscape}
\usepackage{makecell}
\usepackage{multirow}

\DeclareMathOperator{\E}{\mathbb{E}}

\usepackage{verbatim}
\usepackage{bigfoot}

\usepackage{dashbox}
\newcommand\dboxed[1]{\dbox{\ensuremath{#1}}}

\newcommand*\xbar[1]{%
  \hbox{%
    \vbox{%
      \hrule height 0.5pt % The actual bar
      \kern0.5ex%         % Distance between bar and symbol
      \hbox{%
        \kern-0.1em%      % Shortening on the left side
        \ensuremath{#1}%
        \kern-0.1em%      % Shortening on the right side
      }%
    }%
  }%
}
\usepackage{titlesec}
\titleformat{\section}
  {\large\scshape}{\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\scshape}{\thesubsection}{1em}{}
\titleformat{\subsubsection}
  {\normalfont\scshape}{\thesubsubsection}{1em}{}

\usepackage[format=plain,
            labelfont={bf,it},
            textfont=it]{caption}

\usepackage[bottom]{footmisc}

\endinput